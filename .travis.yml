os:
  - linux
language: node_js
node_js:
  - '8.3'
addons:
  apt:
    sources:
    - google-chrome
    packages:
    - google-chrome-stable
branches:
  only:
  - master
before_install:
  - cd frontend
  - npm install -g @angular/cli@7.1.4
script:
  - ng test -no-progress --browsers=ChromeHeadlessNoSandbox 
  - ng e2e
  - ng build
